#include<stdio.h>
int main()
{
    int atd;
    printf("ENter Stduednt Atd:",atd);
    scanf("%d",&atd);
    if(atd>=80)
    {
        printf("allow in hall");
    }
    if(atd<80)
    {
    printf("not allow in hall");
    }
    return 0;
}