#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<conio.h>
int main()
{
    srand(time(NULL));
printf("\n%d",rand());
printf("\n%d",rand());
printf("\n%d",rand());
printf("\n%d",rand());
}
